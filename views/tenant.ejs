<%- include('partials/header') -%>

<%- tenant.firstName || tenant["First Name"] %>

<table class="table">
  <% for(const property in tenant) { %>
  <% if(tenant[property]) { %>
  <tr>
    <th><%= property %> </th>
    <td> <%= tenant[property] %> </td>

  </tr>
  <% } %>
  <% } %>
</table>

</div>
</div>

<%- include('partials/footer') -%>